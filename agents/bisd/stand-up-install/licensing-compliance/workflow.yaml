version: "1.0"
artifact_type: workflow_definition
swarm: bisd
agent: licensing-compliance
tenant: basco
status: STUB               # implement during build Phase 3

name: licensing_compliance_workflow
display_name: "Licensing Compliance Workflow"
description: >
  Workflow for Licensing Compliance.
  Business object: Licenses and permits.
  Posture: Sign-off.

trigger:
  type: event
  source: "Event: per jurisdiction and job"

is_active: false           # workflow does not run until Config Registry is_active = true

steps:
      - id: execute
        agent: licensing_compliance_agent
        action: "STUB — define during build Phase 3"
        output: licensing_compliance_result


# ── Thresholds (override via Config Registry) ─────────────────────────────────
thresholds:
  sla_hours: null          # TBD — to be confirmed during build Phase 5
  escalate_after_hours: null

# ── Outputs ───────────────────────────────────────────────────────────────────
outputs:
  - trust_ledger:
      action: log_decision
      always: true
  - cloud_logging:
      log_name: isrds/bisd/licensing-compliance
