version: "1.0"
artifact_type: agent_spec
swarm: bisd
tenant: basco
generated_by: isrds-platform
status: STUB               # set to ACTIVE only when is_active: true in Config Registry

# ── Agent identity ─────────────────────────────────────────────────────────────
name: insurance_compliance_agent
display_name: "Insurance Compliance"
description: >
  Insurance Compliance agent. Produces: Insurance certificates.

sub_department: "stand-up-install"
business_object: "Insurance certificates"
agent_type: single
posture: "Sign-off"
blocked_until: "None"
implementation_status: "NEW"

# ── Runtime activation gate ───────────────────────────────────────────────────
is_active: false           # NEVER set to true without explicit go-ahead
activation_gate: MUST_APPROVE_BEFORE_LIVE

model: null                # resolved at runtime via config_registry.resolve(tenant_id, agent_id)
runtime: google-adk

# ── Authority gradient ────────────────────────────────────────────────────────
authority_gradient:
  DECIDE_AND_REPORT:
    applies_to: [all_reads, all_analysis, log_decision]
  MUST_ESCALATE:
    applies_to: [all_writes, all_external_actions, all_commitments]
  DECIDE_SILENTLY:
    applies_to: []          # none — everything is logged

# ── Human gates ───────────────────────────────────────────────────────────────
human_gates:
    - Review
    - Escalate

# ── Runtime layers ────────────────────────────────────────────────────────────
runtime_layers:
  1_config_registry:
    status: stub
    implementation: ../shared/config_registry.py
    source: "Firestore system_config/bisd_agent_config"

  2_agent_runtime:
    status: stub
    implementation: google-adk LlmAgent + Runner

  3_tools:
    status: stub
    implementation: ../shared/tool_registry.py
    artifact: tool_registry.yaml
    integrations: "COI and insurance verification sources"

  4_memory:
    status: stub
    implementation: ../shared/memory.py
    backend: Vertex AI Agent Engine
    artifact: memory_schema.yaml

  5_policy_governance:
    status: stub
    implementation: ../shared/governance.py + governance-rules.yaml

  6_human_override:
    status: stub
    gate_types: [Review, Escalate]

  7_workflow:
    status: stub
    artifact: workflow.yaml
    trigger: "Event: per job and per sub"

  8_observability:
    status: stub
    implementation: ../shared/observability.py
    destinations:
      - Cloud Logging (logName: isrds/bisd/insurance-compliance)
      - Cloud Monitoring (custom.googleapis.com/isrds/bisd/insurance-compliance/*)

# ── Notes for build ───────────────────────────────────────────────────────────
spec_notes: >
  HARD GATE: Insurance Compliance before Dispatch. Basco's COI to GC; subs' COI to Basco.

# ── Portable artifacts ────────────────────────────────────────────────────────
artifacts:
  - agent_spec.yaml          # this file
  - prompts/agent.md
  - tool_registry.yaml       # stub — endpoints TBD pending vendor access
  - memory_schema.yaml       # stub — fields TBD pending requirements
  - governance-rules.yaml
  - workflow.yaml
