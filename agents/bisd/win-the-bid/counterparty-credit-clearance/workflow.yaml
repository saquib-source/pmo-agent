version: "1.0"
artifact_type: workflow_definition
swarm: bisd
agent: counterparty-credit-clearance
tenant: basco
status: STUB               # implement during build Phase 3

name: counterparty_credit_clearance_workflow
display_name: "Counterparty Credit Clearance Workflow"
description: >
  Workflow for Counterparty Credit Clearance.
  Business object: Counterparty credit.
  Posture: Sign-off.

trigger:
  type: event
  source: "Event: bid (light) and award (firm)"

is_active: false           # workflow does not run until Config Registry is_active = true

steps:
      - id: execute
        agent: counterparty_credit_clearance_agent
        action: "STUB — define during build Phase 3"
        output: counterparty_credit_clearance_result


# ── Thresholds (override via Config Registry) ─────────────────────────────────
thresholds:
  sla_hours: null          # TBD — to be confirmed during build Phase 5
  escalate_after_hours: null

# ── Outputs ───────────────────────────────────────────────────────────────────
outputs:
  - trust_ledger:
      action: log_decision
      always: true
  - cloud_logging:
      log_name: isrds/bisd/counterparty-credit-clearance
