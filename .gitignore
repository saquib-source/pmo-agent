# Python virtual environments
.venv/
venv/
env/

# Python cache / compiled
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Environment files (NEVER commit secrets)
.env
.env.*
!.env.template
!.env.example

# JSON (selective)
*.json
!**/package.json
!**/tsconfig.json
!**/memory-schema.json
# Agent runtime configs are versioned core artifacts (vendor-neutral, no secrets):
# the Config Registry (engines.json) and source configs. Never put credentials in
# these files — secrets live in .env (ignored) / GCP Secret Manager.
!agents/*/config/*.json
!agents/*/config/sources/*.json
!**/tool-registry.json

# JSONL
trust-ledger.jsonl

# Databases
*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log

# Certificates / secrets / service account keys
*.pem
*.key
*.crt
*.p12
isr-division-systems-*.json

# ADK runtime artifacts
.adk/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Briefs (runtime generated)
**/briefs/

# Node (if applicable)
node_modules/