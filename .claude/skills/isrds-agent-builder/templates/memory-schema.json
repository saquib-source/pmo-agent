{
  "apiVersion": "isrds.agent/v1",
  "kind": "MemorySchema",
  "agent_id": "REPLACE-must-match-spec",
  "store": "vertex_agent_engine",
  "tenant_isolation": true,
  "fields": [
    {
      "name": "REPLACE",
      "type": "REPLACE",
      "purpose": "REPLACE — must be genuine cross-run agent context",
      "destination_justification": "agent state between runs — correctly in Agent Engine"
    }
  ],
  "explicitly_not_in_memory": [
    {
      "data": "REPLACE — e.g. business records this agent touches",
      "correct_destination": "postgres",
      "reason": "durable relational records belong in PostgreSQL, not Agent Engine memory"
    }
  ]
}
